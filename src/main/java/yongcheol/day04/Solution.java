package yongcheol.day04;

public class Solution {

//    void dfs(int idx, int depth, boolean[] used, int row, int answer, String[][] relation) {
//        if (used[idx]) {
//            return ;
//        }
//        for (int i = 0; i < depth; i++) {
//            for (relation[idx][0])
//        }
//    }

    public int solution(String[][] relation) {
        int answer = 0;
        int col = relation.length;
        int row = relation[0].length;
        boolean[] used = new boolean[col];

//        for (int i = 0; i < col; i++) {
//            dfs(i, 1, used, row, answer, relation);
//        }
        return answer;
    }
}
